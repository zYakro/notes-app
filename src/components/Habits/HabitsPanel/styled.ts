import { View } from "react-native";
import styled from "styled-components";

export const HabitsPanelContainer = styled(View)`
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100%;
  height: 70%;
  flex: 1;
`