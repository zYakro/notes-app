import { View } from "react-native";
import styled from "styled-components";

export const DraggablePanelContainer = styled(View)`
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  height: 100%;
`